<% @user.bucket_list_achievements.each do |achievement| %>

    <%= achievement.description %>

    <% if @user == current_user %>
        <%= link_to 'Ta bort', remove_bucket_list_item_path(achievement.id), id: 'remove-achievement' %>
    <% end %>

    <br>
<% end %><br>