<% if already_in_bucket_list(achievement) %>
	<%= link_to 'Remove from Bucket list', remove_bucket_list_item_path(achievement.id), id: 'remove-achievement' %>
<% else %>
	<%= link_to 'Add to Bucket list', add_achievement_bucket_list_path(achievement.id), id: 'add-achievement' %><br>
<% end %>